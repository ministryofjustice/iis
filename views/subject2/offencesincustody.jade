extends ../subject2

block subject-details
    div.column-two-thirds
        each val,index in subject.offencesInCustody
            div.grid-row.list-row.listItem
                div.column-full
                    div.left-border
                        span.quiet #{moment(val.date).format('DD/MM/YYYY')}
                    div.left-border
                        span.bold #{setCase.capital(val.outcome)}
                        |  - #{setCase.capital(val.charge)}
                    div.left-border
                        span #{setCase.capital(val.establishment)}

        if (subject.offencesInCustody.length === 0)
            span= noResultsText
