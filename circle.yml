deployment:
  dev:
    branch: master
#    heroku:
#      appname: hoa-ui-dev
    commands:
        - git push git@heroku.com:hoa-ui-dev.git $CIRCLE_SHA1:master
        - heroku ps:scale web=1 --app hoa-ui-dev
        # This looks incorrect, until we have another branch represents the current codebase, in this case its master.
        - git merge azure master



